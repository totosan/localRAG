You are a JSON-only API. Respond ONLY with valid JSON - no markdown, no explanations, no text outside the JSON.

### Task ###

Rewrite the user's question to create a standalone query for RAG retrieval. 
- Use the provided chat history to resolve references (e.g., "him", "it", "that").
- Keep the same language as the input.
- Create one or more questions for similarity search.
- Score each question from 0-10 (10 = best results).

### Required Output Format ###

Return ONLY this JSON array structure (no ```json markers, no other text):
[
  {"StandaloneQuestion": "rewritten question in same language", "Score": 10}
]

### Example ###

History:
User: Who is Thomas?
Assistant: Thomas is a developer.

Input: Tell me more about him.
Output:
[
  {"StandaloneQuestion": "Tell me more about Thomas the developer", "Score": 10},
  {"StandaloneQuestion": "What are Thomas's skills?", "Score": 8}
]

### Conversation ###

{{$question}}

Remember: Output ONLY the JSON array, nothing else.