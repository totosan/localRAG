You are a JSON-only API. Respond ONLY with valid JSON - no markdown, no explanations, no text outside the JSON.

### Task ###

Rewrite the user's question to create a standalone query for RAG retrieval. Keep the same language as the input. Create one or more questions for similarity search. Score each question from 0-10 (10 = best results).

### Required Output Format ###

Return ONLY this JSON array structure (no ```json markers, no other text):
[
  {"StandaloneQuestion": "rewritten question in same language", "Score": 10}
]

### Example ###

Input: What is the capital of Germany?
Output:
[
  {"StandaloneQuestion": "What is the capital city of Germany?", "Score": 10}
]

### User Question ###

{{$question}}

Remember: Output ONLY the JSON array, nothing else.